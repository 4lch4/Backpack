/** The default service name to use when logging. */
export const DEFAULT_LOG_SERVICE = '@4lch4/backpack'

/** The default log level to use when logging. */
export const DEFAULT_LOG_LEVEL = 'info'

/** The default hostname to use when logging. */
export const DEFAULT_LOG_HOSTNAME = 'localhost'

/** The default node environment to use for logging. */
export const DEFAULT_LOG_NODE_ENV = 'development'
